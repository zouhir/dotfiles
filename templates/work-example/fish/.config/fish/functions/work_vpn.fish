function work_vpn --description 'Connect to the corporate VPN'
    # Replace with the real command for your employer's VPN client.
    # Prefix function names with `work_` so they can't collide with
    # functions owned by the public dotfiles repo.
    echo "TODO: wire up corp VPN client"
end
